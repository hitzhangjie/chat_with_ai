VSCode Copilot capabilities:

---

I am **GitHub Copilot**, an expert AI programming assistant powered by the `gemma4:26b` model. I am here to help you develop, debug, and understand your project.

Here is a summary of how I can assist you in this workspace:

### 🛠️ Coding & Development
*   **Code Implementation**: I can implement new features, write unit tests, or refactor existing code.
*   **File Editing**: I can modify existing files using precise tools (`replace_string_in_file`, `insert_edit_into_file`) to ensure accuracy.
*   **New Files & Structure**: I can create new files, directories, or even entire project scaffolds (`create_file`, `create_new_workspace`).
*   **Refactoring**: I can help clean up complex logic, improve performance, or update code to newer standards.

### 🔍 Exploration & Debugging
*   **Codebase Search**: I can search for specific symbols, strings, or patterns across your entire project (`semantic_search`, `grep_search`, `file_search`).
*   **Error Analysis**: I can identify and help fix compilation or linting errors (`get_errors`).
*   **Code Understanding**: I can explain complex functions, classes, or the overall architecture of your project.
*   **Terminal Access**: I can run build commands, tests, or any shell scripts in your terminal (`run_in_terminal`).

### 🧠 Knowledge & Context
*   **Memory Management**: I can store and retrieve information using a multi-tier memory system:
    *   **User Memory**: Persistent preferences and patterns across all sessions.
    *   **Session Memory**: Task-specific context for our current conversation.
    *   **Repository Memory**: Project-specific facts and conventions.
*   **External Research**: I can fetch information from the web or search GitHub repositories to find solutions or documentation (`fetch_webpage`, `github_repo`).
*   **Task Tracking**: For complex tasks, I can maintain a structured `todo` list to track progress (`manage_todo_list`).

### 🧪 Specialized Tools
*   **Jupyter Notebooks**: I can edit and execute cells within `.ipynb` files (`edit_notebook_file`, `run_notebook_cell`).
*   **Visualizations**: I can render Mermaid diagrams to help visualize architectures or workflows (`renderMermaidDiagram`).
*   **VS Code API**: If you are developing a VS Code extension, I can use specialized tools to browse the official API documentation (`get_vscode_api`).

**How to get started:**
Simply ask me to "find where X is defined", "implement a new shader for Y", "fix the errors in Z", or "create a todo list for implementing feature A".
